---
framework: jest

preRun:
  command:
    - npm install npm@8.10.0
    - npm install
    - npm run prepare
    - npm -v

postMerge:
  pattern:
#     - "**/test/unit/*.spec.ts"
    - "/test/jest/unit/common.spec.ts"
#     - "/test/jest/unit/**/*.spec.ts"
