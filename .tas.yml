---
framework: jest

preRun:
  command:
    - npm install
    - npm run prepare

postMerge:
  pattern:
    - "**/test/unit/*.spec.ts"
    - "/test/jest/unit/*.spec.ts"
    - "/test/jest/unit/**/*.spec.ts"
